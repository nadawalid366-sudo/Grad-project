PORT=5001
MONGODB_DB=gradproject2
MONGO_URI=mongodb+srv://admin:admingradp1@cluster0gradp.hfykgzn.mongodb.net/?appName=Cluster0gradp
# EXPO_PUBLIC_API_URL is intentionally not set: the app auto-detects the
# backend IP from the Expo dev-server host, so it keeps working when your
# network/IP changes. Set it explicitly only to override that behavior.
# ALLOWED_ORIGIN left unset => backend allows all origins (dev).
